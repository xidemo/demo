# Codeception Test Suite Configuration
#
# Suite for unit (internal) tests.

class_name: UnitTester
modules:
    enabled:
        - Symfony:
        - Doctrine2:
            depends: Symfony
        - Asserts
        - \Helper\Unit
